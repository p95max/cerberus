default_type text/plain;

server {
    listen 80;
    server_name _;

    location = /healthz {
        return 200 "ok\n";
    }

    location / {
        return 200 "Cerberus development edge is running.\n";
    }
}
