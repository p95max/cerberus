server {
    listen 80;
    server_name _;
    default_type text/plain;

    location = /healthz {
        return 200 "ok\n";
    }

    location / {
        return 200 "Cerberus development edge is running.\n";
    }
}
